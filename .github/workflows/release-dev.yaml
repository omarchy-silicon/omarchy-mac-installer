name: release-dev

on:
  # Omarchy fork dev deployment requires owner-authorized dispatch until its signed delivery pipeline exists.
  workflow_dispatch:

jobs:
  release-dev:
    uses: ./.github/workflows/release.yaml
    with:
      upload-type: installer-dev
    secrets: inherit
